---
const components = [
  {
    title: 'Streaming price feeds',
    description:
      'Request a dataline to stream real time price updates. Fast local queries with pre-aggregated bars - from 5 seconds to yearly resolutions. Extended hours and pre-market included.',
  },
  {
    title: 'Direct broker access',
    description:
      'Bring your own broker. Your cash and assets stay in your name, at your broker — not held by a intermediary platform that can freeze withdrawals or collapse overnight. Direct connection gives you full real time control over execution and positions.',
  },
  {
    title: 'Signal data',
    description:
      'Technical indicators, financial metrics and market trend signals. Vectorised computation for your entire universe of instruments, instant local access. Need a custom indicator? Add it to the computation pipeline.',
  },
  {
    title: 'Risk engine',
    description:
      'Kill switches and position controls activate when conditions are met - drawdown limits, volatility thresholds or earnings proximity without P&L buffer. Parameterise your rules once and let the system hold the line',
  },
  {
    title: 'Trading agent orchestration',
    description:
      'Deploy trading agents, each running as an isolated client with its own capital allocation, risk limits, and internal state. All orders are routed through a central message bus to the shared risk engine and execution layer — ensuring global exposure limits are enforced, regardless of how many agents are running.',
  },
  {
    title: 'Smart triggers',
    description:
      'Plan trades using conditional triggers. Chain them across entry, add, reduce, and exit to orchestrate the full lifecycle. Glasshouse watches the conditions and submits orders only when they are met — keeping your levels hidden from the book until execution.',
  },
];
---

<section id="architecture" class="px-6 py-24">
  <div class="mx-auto max-w-6xl">
    <h2 class="text-center text-3xl font-bold tracking-tight text-text-primary sm:text-4xl">
      What's behind the glass
    </h2>
    <p class="mx-auto mt-4 max-w-2xl text-center text-text-secondary">
        Glasshouse is a headless trading automation platform. Scan signals, analyse price action, size positions, execute orders, monitor exposure, and scale out when conditions change.
    </p>

    <div class="mt-16 grid gap-8 md:grid-cols-2">
      {components.map((component) => (
        <div class="rounded-xl border border-accent/10 bg-bg-elevated p-8 transition-colors hover:border-accent/30">
          <h3 class="text-xl font-semibold text-accent">{component.title}</h3>
          <p class="mt-4 leading-relaxed text-text-secondary">{component.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
