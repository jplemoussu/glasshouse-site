---
const tools = [
  { name: '/portfolio', description: 'Positions, exposure, P&L, allocation — queryable in real time.' },
  { name: '/smart-triggers', description: 'Read, create, and manage conditional triggers programmatically.' },
  { name: '/traders', description: 'Monitor and control autonomous trading agents. Access traders context store.' },
];
---

<section class="border-y border-border bg-bg-elevated px-6 py-24">
  <div class="mx-auto grid max-w-6xl gap-12 md:grid-cols-2 md:items-center">
    <!-- Left: copy -->
    <div>
      <h2 class="text-2xl font-bold tracking-tight text-text-primary sm:text-3xl">
        Plug in AI control
      </h2>
      <p class="mt-6 leading-relaxed text-text-secondary">
        Glasshouse exposes live platform state — positions, triggers, agents — as MCP tools. Connect Claude, ChatGPT, or any local model and compose tools into reusable skills: "check my portfolio, flag anything down more than 5%, tighten stops on those positions." One prompt, repeatable on demand.
      </p>
    </div>

    <!-- Right: tool list -->
    <div class="rounded-xl border border-border bg-bg-card p-8">
      <p class="text-sm font-medium text-text-muted">Available MCP tools</p>
      <ul class="mt-6 space-y-4">
        {tools.map((tool) => (
          <li>
            <code class="text-sm font-semibold text-accent">{tool.name}</code>
            <p class="mt-1 text-sm leading-relaxed text-text-secondary">{tool.description}</p>
          </li>
        ))}
      </ul>
    </div>
  </div>
</section>
